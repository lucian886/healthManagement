# 健康管理系统 - 简易部署指南

## 🎯 部署流程

```
1. 前端打包 → dist 文件夹
2. 后端打包 → jar 包
3. 配置 Nginx → 指向 dist 和代理后端
4. 启动服务 → 后端 + AI 服务
```

---

## 📦 第一步：构建前端 (生成 dist 文件夹)

### 本地构建（推荐）

```bash
cd /Users/liuzixuan/healthManagement/frontend

# 安装依赖（如果还没安装）
npm install

# 打包生成 dist 文件夹
npm run build
```

构建完成后，会生成 `frontend/dist/` 文件夹，包含：
- `index.html`
- `assets/` (JS、CSS 等静态资源)

### 上传到服务器

```bash
# 将 dist 文件夹上传到服务器
scp -r frontend/dist root@服务器IP:/opt/health-management/frontend/

# 或者打包后上传
cd frontend
tar -czf dist.tar.gz dist/
scp dist.tar.gz root@服务器IP:/opt/health-management/frontend/
# 在服务器上解压
# ssh root@服务器IP
# cd /opt/health-management/frontend && tar -xzf dist.tar.gz
```

---

## 📦 第二步：构建后端 (生成 jar 包)

### 本地构建

```bash
cd /Users/liuzixuan/healthManagement/backend

# 使用 Maven 构建（跳过测试）
./mvnw clean package -DskipTests

# 或者如果有全局 Maven
mvn clean package -DskipTests
```

构建完成后，jar 包位置：
```
backend/target/health-management-0.0.1-SNAPSHOT.jar
```

### 上传到服务器

```bash
# 上传 jar 包
scp backend/target/health-management-0.0.1-SNAPSHOT.jar \
    root@服务器IP:/opt/health-management/backend/

# 同时上传配置文件
scp backend/src/main/resources/application-prod.yml \
    root@服务器IP:/opt/health-management/backend/
```

---

## 🔧 第三步：配置 Nginx

### 创建 Nginx 配置文件

在服务器上创建配置文件：

```bash
sudo vi /etc/nginx/sites-available/health-management
```

粘贴以下配置：

```nginx
server {
    listen 80;
    server_name localhost;  # 改成你的域名或 IP
    
    # 前端静态文件目录
    root /opt/health-management/frontend/dist;
    index index.html;
    
    # 上传文件大小限制
    client_max_body_size 10M;
    
    # 前端路由 - SPA 支持
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8081/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 启用配置

```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/health-management /etc/nginx/sites-enabled/

# 删除默认配置（可选）
sudo rm /etc/nginx/sites-enabled/default

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

---

## 🚀 第四步：启动服务

### 1. 启动后端服务

```bash
# SSH 登录到服务器
ssh root@服务器IP

# 进入后端目录
cd /opt/health-management/backend

# 后台启动后端（方式一：使用 nohup）
nohup java -jar health-management-0.0.1-SNAPSHOT.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &

# 查看进程
ps aux | grep java

# 查看日志
tail -f backend.log
```

**或者使用屏幕会话（方式二）：**

```bash
# 创建新会话
screen -S backend

# 启动后端
cd /opt/health-management/backend
java -jar health-management-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod

# 按 Ctrl+A 然后按 D 离开会话（服务继续运行）
# 重新连接: screen -r backend
```

### 2. 启动 AI 服务

```bash
# 进入 AI 服务目录
cd /opt/health-management/ai-service

# 安装依赖（首次需要）
pip3 install -r requirements.txt

# 配置环境变量
export DASHSCOPE_API_KEY="你的通义千问API密钥"

# 后台启动 AI 服务
nohup python3 main.py > ai-service.log 2>&1 &

# 查看进程
ps aux | grep python3

# 查看日志
tail -f ai-service.log
```

---

## ✅ 验证部署

### 1. 检查服务状态

```bash
# 检查后端（应该返回 8081）
netstat -tulnp | grep 8081

# 检查 AI 服务（应该返回 8001）
netstat -tulnp | grep 8001

# 检查 Nginx（应该返回 80）
netstat -tulnp | grep 80
```

### 2. 测试接口

```bash
# 测试后端
curl http://localhost:8081/api/auth/health

# 测试 AI 服务
curl http://localhost:8001/health

# 测试 Nginx 代理
curl http://localhost/api/auth/health
```

### 3. 浏览器访问

打开浏览器访问：
```
http://服务器IP
```

应该能看到前端页面！

---

## 📁 服务器目录结构

```
/opt/health-management/
├── frontend/
│   └── dist/                          # 前端静态文件
│       ├── index.html
│       └── assets/
├── backend/
│   ├── health-management-0.0.1-SNAPSHOT.jar   # 后端 jar 包
│   ├── application-prod.yml           # 配置文件
│   ├── backend.log                    # 后端日志
│   └── uploads/                       # 上传文件目录
└── ai-service/
    ├── main.py
    ├── requirements.txt
    ├── ai-service.log                 # AI 日志
    └── venv/                          # Python 虚拟环境（可选）
```

---

## 🔄 更新代码

### 更新前端

```bash
# 本地重新打包
cd /Users/liuzixuan/healthManagement/frontend
npm run build

# 上传到服务器（会覆盖旧文件）
scp -r dist/* root@服务器IP:/opt/health-management/frontend/dist/

# 清除浏览器缓存，刷新页面即可看到更新
```

### 更新后端

```bash
# 本地重新打包
cd /Users/liuzixuan/healthManagement/backend
./mvnw clean package -DskipTests

# 上传到服务器
scp target/health-management-0.0.1-SNAPSHOT.jar \
    root@服务器IP:/opt/health-management/backend/

# 在服务器上重启后端
# 1. 找到进程 ID
ps aux | grep health-management

# 2. 停止旧进程
kill <PID>

# 3. 启动新版本
cd /opt/health-management/backend
nohup java -jar health-management-0.0.1-SNAPSHOT.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &
```

---

## 🛑 停止服务

```bash
# 停止后端
ps aux | grep health-management
kill <后端进程ID>

# 停止 AI 服务
ps aux | grep "python3 main.py"
kill <AI进程ID>

# 重启 Nginx
sudo systemctl restart nginx
```

---

## 📝 快速启停脚本（可选）

创建便捷脚本来管理服务：

### 启动脚本 `start-services.sh`

```bash
#!/bin/bash

cd /opt/health-management

# 启动后端
cd backend
nohup java -jar health-management-0.0.1-SNAPSHOT.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &
echo "✅ 后端已启动"

# 启动 AI 服务
cd ../ai-service
export DASHSCOPE_API_KEY="你的API密钥"
nohup python3 main.py > ai-service.log 2>&1 &
echo "✅ AI 服务已启动"

# 显示进程
sleep 2
ps aux | grep -E "health-management|main.py" | grep -v grep
```

### 停止脚本 `stop-services.sh`

```bash
#!/bin/bash

# 停止后端
pkill -f health-management-0.0.1-SNAPSHOT.jar
echo "✅ 后端已停止"

# 停止 AI 服务
pkill -f "python3 main.py"
echo "✅ AI 服务已停止"
```

使用方式：

```bash
chmod +x start-services.sh stop-services.sh

# 启动
./start-services.sh

# 停止
./stop-services.sh
```

---

## 🔍 常见问题

### 1. 前端页面空白

```bash
# 检查 dist 目录
ls -la /opt/health-management/frontend/dist/

# 检查 Nginx 配置
sudo nginx -t
sudo cat /etc/nginx/sites-available/health-management

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/error.log
```

### 2. API 请求失败

```bash
# 检查后端是否运行
ps aux | grep java

# 检查端口
netstat -tulnp | grep 8081

# 查看后端日志
tail -f /opt/health-management/backend/backend.log

# 测试后端直接访问
curl http://localhost:8081/api/auth/health
```

### 3. 跨域问题

如果前端和后端不在同一域名，需要在后端配置 CORS。
后端已配置 CORS，允许所有来源，所以通常不会有问题。

### 4. 端口被占用

```bash
# 查看端口占用
sudo lsof -i :80
sudo lsof -i :8081
sudo lsof -i :8001

# 停止占用进程
sudo kill <PID>
```

---

## 🔒 生产环境建议

### 1. 使用环境变量文件

创建 `/opt/health-management/.env`：

```bash
DASHSCOPE_API_KEY=你的API密钥
ALIYUN_OSS_ACCESS_KEY_ID=你的OSS密钥ID
ALIYUN_OSS_ACCESS_KEY_SECRET=你的OSS密钥Secret
```

启动时加载：

```bash
source /opt/health-management/.env
nohup java -jar health-management-0.0.1-SNAPSHOT.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &
```

### 2. 配置 HTTPS

```bash
# 安装 Certbot
sudo apt-get install certbot python3-certbot-nginx

# 申请证书
sudo certbot --nginx -d your-domain.com
```

### 3. 防火墙配置

```bash
# Ubuntu
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```

---

## 📊 部署检查清单

构建阶段：
- [ ] 前端打包完成，生成 `dist/` 文件夹
- [ ] 后端打包完成，生成 `.jar` 文件

上传阶段：
- [ ] `dist/` 文件夹已上传到服务器
- [ ] `.jar` 文件已上传到服务器
- [ ] 配置文件已上传

配置阶段：
- [ ] Nginx 配置文件已创建
- [ ] Nginx 配置测试通过 (`nginx -t`)
- [ ] Nginx 已重启

启动阶段：
- [ ] 后端服务已启动（端口 8081）
- [ ] AI 服务已启动（端口 8001）
- [ ] Nginx 运行正常（端口 80）

验证阶段：
- [ ] 可以访问前端页面
- [ ] 用户可以注册登录
- [ ] API 请求正常
- [ ] 聊天功能正常

---

## 🎉 完成！

现在你的部署方式非常简单：

1. **本地打包** → `npm run build` + `mvn package`
2. **上传文件** → `scp` 上传 dist 和 jar
3. **配置 Nginx** → 指向 dist，代理到后端
4. **启动服务** → `nohup java -jar` + `nohup python3 main.py`

访问 `http://服务器IP` 即可！







