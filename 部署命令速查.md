# 部署命令速查表

## 🔨 本地构建

### 一键构建（推荐）
```bash
cd /Users/liuzixuan/healthManagement
./build-local.sh
```

### 手动构建

**前端：**
```bash
cd /Users/liuzixuan/healthManagement/frontend
npm install          # 首次或依赖更新时
npm run build        # 生成 dist/ 文件夹
tar -czf dist.tar.gz dist/  # 打包（方便上传）
```

**后端：**
```bash
cd /Users/liuzixuan/healthManagement/backend
mvn clean package -DskipTests  # 生成 jar 包
# 产物: backend/target/health-management-1.0.0.jar
```

---

## 📤 上传到服务器

```bash
# 上传前端
scp /Users/liuzixuan/healthManagement/frontend/dist.tar.gz \
    root@服务器IP:/opt/health-management/frontend/

# 上传后端
scp /Users/liuzixuan/healthManagement/backend/target/health-management-1.0.0.jar \
    root@服务器IP:/opt/health-management/backend/

# 上传 AI 服务（首次部署）
cd /Users/liuzixuan/healthManagement/ai-service
tar -czf ai-service.tar.gz *.py requirements.txt
scp ai-service.tar.gz root@服务器IP:/opt/health-management/
```

---

## 🚀 服务器端部署

### 首次部署

```bash
# SSH 登录服务器
ssh root@服务器IP

# 1. 解压前端
cd /opt/health-management/frontend
tar -xzf dist.tar.gz

# 2. 配置 Nginx
sudo vi /etc/nginx/sites-available/health-management
# 粘贴以下内容：

server {
    listen 80;
    server_name localhost;
    root /opt/health-management/frontend/dist;
    index index.html;
    client_max_body_size 10M;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://127.0.0.1:8081/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# 启用配置
sudo ln -s /etc/nginx/sites-available/health-management /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# 3. 启动后端
cd /opt/health-management/backend
nohup java -jar health-management-1.0.0.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &

# 4. 启动 AI 服务
cd /opt/health-management/ai-service
export DASHSCOPE_API_KEY="你的API密钥"
pip3 install -r requirements.txt
nohup python3 main.py > ai-service.log 2>&1 &

# 5. 验证
ps aux | grep -E "health-management|main.py"
curl http://localhost/api/auth/health
```

---

## 🔄 更新部署

### 更新前端

```bash
# 本地
cd /Users/liuzixuan/healthManagement/frontend
npm run build
tar -czf dist.tar.gz dist/
scp dist.tar.gz root@服务器IP:/opt/health-management/frontend/

# 服务器
ssh root@服务器IP
cd /opt/health-management/frontend
rm -rf dist
tar -xzf dist.tar.gz
# 浏览器清缓存刷新即可
```

### 更新后端

```bash
# 本地
cd /Users/liuzixuan/healthManagement/backend
mvn clean package -DskipTests
scp target/health-management-1.0.0.jar root@服务器IP:/opt/health-management/backend/

# 服务器
ssh root@服务器IP

# 停止旧进程
ps aux | grep health-management-1.0.0.jar
kill <PID>
# 或使用: pkill -f health-management-1.0.0.jar

# 启动新版本
cd /opt/health-management/backend
nohup java -jar health-management-1.0.0.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &

# 查看日志
tail -f backend.log
```

### 更新 AI 服务

```bash
# 本地
cd /Users/liuzixuan/healthManagement/ai-service
scp main.py agent.py tools.py config.py root@服务器IP:/opt/health-management/ai-service/

# 服务器
ssh root@服务器IP
pkill -f "python3 main.py"
cd /opt/health-management/ai-service
export DASHSCOPE_API_KEY="你的API密钥"
nohup python3 main.py > ai-service.log 2>&1 &
```

---

## 🔍 服务管理

### 查看服务状态

```bash
# 查看进程
ps aux | grep -E "health-management|main.py|nginx"

# 查看端口
netstat -tulnp | grep -E "80|8081|8001"
# 或使用: lsof -i :80 -i :8081 -i :8001
```

### 查看日志

```bash
# 后端日志
tail -f /opt/health-management/backend/backend.log

# AI 服务日志
tail -f /opt/health-management/ai-service/ai-service.log

# Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### 停止服务

```bash
# 停止后端
pkill -f health-management-1.0.0.jar

# 停止 AI 服务
pkill -f "python3 main.py"

# 重启 Nginx
sudo systemctl restart nginx
```

### 重启服务

```bash
# 重启后端
pkill -f health-management-1.0.0.jar
cd /opt/health-management/backend
nohup java -jar health-management-1.0.0.jar --spring.profiles.active=prod > backend.log 2>&1 &

# 重启 AI 服务
pkill -f "python3 main.py"
cd /opt/health-management/ai-service
export DASHSCOPE_API_KEY="你的API密钥"
nohup python3 main.py > ai-service.log 2>&1 &
```

---

## 🧪 测试接口

```bash
# 测试后端
curl http://localhost:8081/api/auth/health

# 测试 AI 服务
curl http://localhost:8001/health

# 测试 Nginx 代理
curl http://localhost/api/auth/health

# 测试前端
curl http://localhost/
```

---

## 📁 文件位置

### 本地
- 前端产物: `/Users/liuzixuan/healthManagement/frontend/dist/`
- 前端压缩包: `/Users/liuzixuan/healthManagement/frontend/dist.tar.gz`
- 后端产物: `/Users/liuzixuan/healthManagement/backend/target/health-management-1.0.0.jar`

### 服务器
- 前端: `/opt/health-management/frontend/dist/`
- 后端: `/opt/health-management/backend/health-management-1.0.0.jar`
- AI 服务: `/opt/health-management/ai-service/`
- Nginx 配置: `/etc/nginx/sites-available/health-management`
- 日志: `/opt/health-management/backend/backend.log`
- 日志: `/opt/health-management/ai-service/ai-service.log`

---

## 🛠️ 故障排查

### 前端页面空白

```bash
# 检查 Nginx 配置
sudo nginx -t
sudo cat /etc/nginx/sites-available/health-management

# 检查文件是否存在
ls -la /opt/health-management/frontend/dist/

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/error.log
```

### API 请求失败

```bash
# 检查后端是否运行
ps aux | grep health-management

# 检查端口
netstat -tulnp | grep 8081

# 查看后端日志
tail -f /opt/health-management/backend/backend.log
```

### 端口被占用

```bash
# 查看端口占用
sudo lsof -i :80
sudo lsof -i :8081
sudo lsof -i :8001

# 停止占用进程
sudo kill <PID>
```

---

## 🔒 环境变量配置

### 创建 .env 文件（服务器）

```bash
sudo vi /opt/health-management/.env
```

内容：
```bash
DASHSCOPE_API_KEY=你的通义千问API密钥
ALIYUN_OSS_ACCESS_KEY_ID=你的OSS密钥ID
ALIYUN_OSS_ACCESS_KEY_SECRET=你的OSS密钥Secret
```

### 使用环境变量启动

```bash
# 加载环境变量
source /opt/health-management/.env

# 启动后端（环境变量会自动传递）
cd /opt/health-management/backend
nohup java -jar health-management-1.0.0.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &

# 启动 AI 服务
cd /opt/health-management/ai-service
nohup python3 main.py > ai-service.log 2>&1 &
```

---

## 📊 完整部署流程

```bash
# ===== 步骤 1: 本地构建 =====
cd /Users/liuzixuan/healthManagement
./build-local.sh

# ===== 步骤 2: 上传文件 =====
scp frontend/dist.tar.gz root@IP:/opt/health-management/frontend/
scp backend/target/health-management-1.0.0.jar root@IP:/opt/health-management/backend/

# ===== 步骤 3: 服务器部署 =====
ssh root@IP

# 解压前端
cd /opt/health-management/frontend && tar -xzf dist.tar.gz

# 配置 Nginx（首次）
sudo vi /etc/nginx/sites-available/health-management
sudo ln -s /etc/nginx/sites-available/health-management /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl restart nginx

# 启动后端
cd /opt/health-management/backend
nohup java -jar health-management-1.0.0.jar --spring.profiles.active=prod > backend.log 2>&1 &

# 启动 AI 服务
cd /opt/health-management/ai-service
export DASHSCOPE_API_KEY="你的密钥"
nohup python3 main.py > ai-service.log 2>&1 &

# ===== 步骤 4: 验证 =====
curl http://localhost/api/auth/health
# 浏览器访问: http://服务器IP
```

---

**完成！** 🎉







