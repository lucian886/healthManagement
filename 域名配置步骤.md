# 域名配置步骤 - health-management.top

## 📋 配置信息

- **域名**：`health-management.top` 和 `www.health-management.top`
- **目标**：映射到健康管理系统
- **服务器端口**：后端 8081，前端通过 Nginx 80 端口访问

---

## 第一步：阿里云 DNS 解析配置

### 1.1 登录阿里云控制台

访问：https://dns.console.aliyun.com/

### 1.2 添加 DNS 解析记录

找到域名 `health-management.top`，点击"解析设置"，添加以下两条记录：

#### 记录 1：主域名

```
记录类型：A
主机记录：@
记录值：你的服务器公网IP（例如：47.94.41.55）
TTL：10分钟
```

点击"确定"保存。

#### 记录 2：www 子域名

```
记录类型：A
主机记录：www
记录值：你的服务器公网IP（例如：47.94.41.55）
TTL：10分钟
```

点击"确定"保存。

### 1.3 等待 DNS 生效

DNS 通常需要 **5-10 分钟**生效。

#### 测试 DNS 是否生效

在本地终端执行：

```bash
# 测试主域名
ping health-management.top

# 测试 www 域名
ping www.health-management.top

# 如果返回你的服务器 IP，说明解析成功！
```

---

## 第二步：服务器 Nginx 配置

### 2.1 SSH 登录服务器

```bash
ssh root@你的服务器IP
```

### 2.2 上传配置文件

有两种方式：

#### 方式一：本地上传配置文件（推荐）

在本地终端执行：

```bash
# 上传配置文件到服务器
scp /Users/liuzixuan/healthManagement/nginx/health-management-domain.conf \
    root@你的服务器IP:/etc/nginx/sites-available/health-management.conf
```

#### 方式二：直接在服务器上创建

在服务器上执行：

```bash
# 创建配置文件
sudo vi /etc/nginx/sites-available/health-management.conf
```

然后复制以下内容：

```nginx
# 后端服务 upstream
upstream health_backend {
    server 127.0.0.1:8081;
    keepalive 32;
}

# AI 服务 upstream
upstream health_ai {
    server 127.0.0.1:8001;
    keepalive 32;
}

server {
    listen       80;
    server_name  health-management.top www.health-management.top;

    # 日志
    access_log  /var/log/nginx/health-management-access.log;
    error_log   /var/log/nginx/health-management-error.log;

    # 前端静态文件
    root   /opt/health-management/frontend/dist;
    index  index.html;

    # 上传文件大小限制
    client_max_body_size 10M;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript 
               application/json application/javascript application/xml+rss;

    # 前端路由 - SPA 支持
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://health_backend/api/;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
    }

    # AI 服务代理
    location /ai/ {
        proxy_pass http://health_ai/;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 错误页面
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
```

保存并退出（`:wq`）。

### 2.3 启用配置

```bash
# 创建软链接
sudo ln -sf /etc/nginx/sites-available/health-management.conf \
            /etc/nginx/sites-enabled/health-management.conf

# 如果之前有旧的 8080 端口配置，可以移除
# sudo rm -f /etc/nginx/sites-enabled/default
```

### 2.4 测试 Nginx 配置

```bash
sudo nginx -t
```

应该显示：
```
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

### 2.5 重启 Nginx

```bash
sudo systemctl reload nginx

# 或完全重启
sudo systemctl restart nginx
```

### 2.6 检查服务状态

```bash
# 检查 Nginx
sudo systemctl status nginx

# 检查后端服务
sudo systemctl status health-backend

# 检查 AI 服务
sudo systemctl status health-ai-service
```

---

## 第三步：配置防火墙和安全组

### 3.1 阿里云安全组

登录阿里云 ECS 控制台，配置安全组规则：

| 协议 | 端口 | 授权对象 | 说明 |
|-----|------|---------|------|
| HTTP | 80 | 0.0.0.0/0 | HTTP 访问 |
| HTTPS | 443 | 0.0.0.0/0 | HTTPS 访问 |
| SSH | 22 | 你的IP/32 | SSH 管理 |

**注意**：8081 和 8001 端口不要对外开放，只允许本地访问！

### 3.2 服务器防火墙

#### Ubuntu (UFW)

```bash
# 允许 HTTP
sudo ufw allow 80/tcp

# 允许 HTTPS
sudo ufw allow 443/tcp

# 允许 SSH
sudo ufw allow 22/tcp

# 启用防火墙
sudo ufw enable

# 查看状态
sudo ufw status
```

#### CentOS (Firewalld)

```bash
# 允许 HTTP 和 HTTPS
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --permanent --add-service=ssh

# 重新加载
sudo firewall-cmd --reload

# 查看状态
sudo firewall-cmd --list-all
```

---

## 第四步：测试访问

### 4.1 HTTP 访问测试

在浏览器中访问：

```
http://health-management.top
http://www.health-management.top
```

应该能看到健康管理系统的登录页面！

### 4.2 API 测试

在服务器上执行：

```bash
# 测试前端
curl http://health-management.top

# 测试后端 API
curl http://health-management.top/api/auth/health

# 应该返回后端的健康检查响应
```

---

## 第五步：配置 HTTPS（推荐）

配置 HTTPS 可以保护用户数据安全，提升 SEO 排名。

### 5.1 安装 Certbot

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y certbot python3-certbot-nginx

# CentOS/RHEL
sudo yum install -y certbot python3-certbot-nginx
```

### 5.2 申请免费 SSL 证书

```bash
# 一键申请并配置（推荐）
sudo certbot --nginx -d health-management.top -d www.health-management.top
```

按照提示操作：

1. **输入邮箱**：用于证书到期提醒
   ```
   Enter email address: your-email@example.com
   ```

2. **同意服务条款**：输入 `Y`

3. **是否分享邮箱**：输入 `N`

4. **HTTP 重定向到 HTTPS**：
   ```
   Please choose whether or not to redirect HTTP traffic to HTTPS:
   1: No redirect
   2: Redirect
   
   选择 2（推荐）
   ```

### 5.3 验证 HTTPS

访问：

```
https://health-management.top
https://www.health-management.top
```

浏览器地址栏应该显示 🔒 锁图标！

### 5.4 自动续期

Certbot 已自动配置续期任务，证书会在到期前自动续期。

测试续期：

```bash
sudo certbot renew --dry-run
```

---

## ✅ 配置完成检查清单

### DNS 配置
- [ ] 已添加 `@` 记录（主域名）
- [ ] 已添加 `www` 记录
- [ ] DNS 解析已生效（`ping` 测试通过）

### Nginx 配置
- [ ] 配置文件已创建
- [ ] `server_name` 已改为 `health-management.top www.health-management.top`
- [ ] Nginx 配置测试通过（`nginx -t`）
- [ ] Nginx 已重启

### 服务状态
- [ ] 后端服务运行正常（8081 端口）
- [ ] AI 服务运行正常（8001 端口）
- [ ] Nginx 运行正常（80 端口）

### 安全配置
- [ ] 阿里云安全组已开放 80、443 端口
- [ ] 服务器防火墙已配置
- [ ] 8081、8001 端口未对外开放

### HTTPS 配置
- [ ] Certbot 已安装
- [ ] SSL 证书申请成功
- [ ] HTTPS 访问正常（显示 🔒）
- [ ] HTTP 自动跳转 HTTPS

### 功能测试
- [ ] 可以访问前端页面
- [ ] 用户可以注册登录
- [ ] API 请求正常
- [ ] 图片上传正常
- [ ] 聊天功能正常

---

## 🔍 故障排查

### 问题 1：域名无法访问

```bash
# 1. 检查 DNS 解析
ping health-management.top

# 2. 检查 Nginx 是否运行
sudo systemctl status nginx

# 3. 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/health-management-error.log

# 4. 检查端口
sudo netstat -tulnp | grep :80
```

### 问题 2：502 Bad Gateway

说明 Nginx 无法连接后端服务。

```bash
# 检查后端服务
sudo systemctl status health-backend

# 查看后端日志
sudo journalctl -u health-backend -n 50

# 测试后端端口
curl http://localhost:8081/api/auth/health
```

### 问题 3：前端页面空白

```bash
# 检查前端文件
ls -la /opt/health-management/frontend/dist/

# 查看 Nginx 日志
sudo tail -f /var/log/nginx/health-management-error.log

# 重新构建前端
cd /opt/health-management/frontend
npm run build
sudo systemctl reload nginx
```

---

## 📊 管理命令

### Nginx 管理

```bash
# 测试配置
sudo nginx -t

# 重新加载（推荐）
sudo systemctl reload nginx

# 完全重启
sudo systemctl restart nginx

# 查看状态
sudo systemctl status nginx

# 查看日志
sudo tail -f /var/log/nginx/health-management-access.log
sudo tail -f /var/log/nginx/health-management-error.log
```

### 服务管理

```bash
# 后端服务
sudo systemctl start health-backend
sudo systemctl stop health-backend
sudo systemctl restart health-backend
sudo systemctl status health-backend

# AI 服务
sudo systemctl start health-ai-service
sudo systemctl stop health-ai-service
sudo systemctl restart health-ai-service
sudo systemctl status health-ai-service

# 查看日志
sudo journalctl -u health-backend -f
sudo journalctl -u health-ai-service -f
```

### SSL 证书管理

```bash
# 查看证书
sudo certbot certificates

# 手动续期
sudo certbot renew

# 测试续期
sudo certbot renew --dry-run

# 撤销证书
sudo certbot revoke --cert-path /etc/letsencrypt/live/health-management.top/cert.pem
```

---

## 🎉 配置完成！

现在你可以通过以下地址访问健康管理系统：

- **HTTP**：http://health-management.top
- **HTTPS**：https://health-management.top
- **带 www**：https://www.health-management.top

---

## 📝 关于博客服务

如果你的博客还在 80 端口运行，需要：

### 方案 1：更换博客端口（推荐）

博客改为 8080 端口，让健康管理系统使用 80 端口：

```nginx
# 博客 - 8080 端口
server {
    listen       8080;
    server_name  localhost;
    
    location / {
        root   /opt/app/hexo/public;
        index  index.html index.htm;
    }
}
```

访问：`http://你的服务器IP:8080`

### 方案 2：使用不同域名

- 博客：使用另一个域名（如 `blog.health-management.top`）
- 健康管理：使用主域名 `health-management.top`

需要在阿里云添加博客子域名解析。

---

**配置时间**：约 20-30 分钟

**有问题随时问我！** 🚀







