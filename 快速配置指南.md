# 快速配置指南 - 域名绑定健康管理系统

## 🎯 需求

- **博客**：不需要域名，通过 IP 访问
- **健康管理**：需要域名 `health-management.top`

---

## 💡 两个方案选择

### 方案一：推荐方案（域名用 80 端口）⭐

**优点**：
- ✅ 访问简洁，不需要加端口号
- ✅ 可以配置 HTTPS（更安全）
- ✅ 符合 Web 标准

**访问方式**：
- 博客：`http://你的服务器IP:8888`
- 健康管理：`http://health-management.top`（自动跳转 HTTPS 后为 `https://health-management.top`）

**配置文件**：`recommended-config.conf`

---

### 方案二：备选方案（健康管理用 8080 端口）

**优点**：
- ✅ 博客保持 80 端口不变

**缺点**：
- ❌ 访问需要加端口号 `:8080`
- ❌ 不能配置标准 HTTPS（需要 443 端口）
- ❌ 需要开放 8080 端口

**访问方式**：
- 博客：`http://你的服务器IP`
- 健康管理：`http://health-management.top:8080`

**配置文件**：`alternative-config.conf`

---

## 🚀 方案一：推荐配置（3步完成）

### 第一步：阿里云 DNS 解析

登录：https://dns.console.aliyun.com/

添加两条 A 记录：

```
记录1：
记录类型：A
主机记录：@
记录值：你的服务器IP

记录2：
记录类型：A
主机记录：www
记录值：你的服务器IP
```

等待 5-10 分钟生效。

---

### 第二步：服务器配置 Nginx

#### 方法A：上传配置文件（推荐）

在本地执行：

```bash
scp /Users/liuzixuan/healthManagement/nginx/recommended-config.conf \
    root@你的服务器IP:/etc/nginx/conf.d/health-management.conf
```

#### 方法B：手动创建配置

SSH 登录服务器：

```bash
ssh root@你的服务器IP
```

创建配置文件：

```bash
sudo vi /etc/nginx/conf.d/health-management.conf
```

复制以下内容：

```nginx
# 后端服务 upstream
upstream health_backend {
    server 127.0.0.1:8081;
    keepalive 32;
}

upstream health_ai {
    server 127.0.0.1:8001;
    keepalive 32;
}

# 博客 - 8888 端口
server {
    listen       8888;
    server_name  localhost;

    access_log  /var/log/nginx/blog-access.log;

    location / {
        root   /opt/app/hexo/public;
        index  index.html index.htm;
    }
}

# 健康管理 - 80 端口（域名）
server {
    listen       80;
    server_name  health-management.top www.health-management.top;

    access_log  /var/log/nginx/health-management-access.log;
    error_log   /var/log/nginx/health-management-error.log;

    root   /opt/health-management/frontend/dist;
    index  index.html;
    client_max_body_size 10M;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://health_backend/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

**修改原有博客配置**：

如果你的博客配置在 `/etc/nginx/conf.d/default.conf` 或其他文件中，需要修改端口：

```bash
# 找到博客配置文件
sudo vi /etc/nginx/conf.d/default.conf

# 或
sudo vi /etc/nginx/nginx.conf
```

把博客的 `listen 80;` 改为 `listen 8888;`

保存并退出。

---

### 第三步：测试并重启 Nginx

```bash
# 测试配置
sudo nginx -t

# 如果显示 "test is successful"，重启 Nginx
sudo systemctl reload nginx

# 查看状态
sudo systemctl status nginx
```

---

### 第四步：访问测试

**博客**：
```
http://你的服务器IP:8888
```

**健康管理**：
```
http://health-management.top
```

---

### 第五步：配置 HTTPS（推荐）

```bash
# 安装 Certbot
sudo apt-get install -y certbot python3-certbot-nginx

# 申请证书
sudo certbot --nginx -d health-management.top -d www.health-management.top

# 选择 2（HTTP 自动跳转 HTTPS）
```

完成后访问：
```
https://health-management.top
```

浏览器应该显示 🔒 安全锁！

---

## 🔧 方案二：备选配置（健康管理用 8080）

### 额外步骤：开放 8080 端口

#### 阿里云安全组

登录阿里云 ECS 控制台，添加规则：

```
协议：TCP
端口：8080
授权对象：0.0.0.0/0
```

#### 服务器防火墙

```bash
# Ubuntu
sudo ufw allow 8080/tcp

# CentOS
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --reload
```

### 配置文件

使用 `alternative-config.conf`：

```bash
scp /Users/liuzixuan/healthManagement/nginx/alternative-config.conf \
    root@你的服务器IP:/etc/nginx/conf.d/health-management.conf
```

或手动创建后，重启 Nginx：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 访问测试

**博客**：
```
http://你的服务器IP
```

**健康管理**：
```
http://health-management.top:8080
```

**注意**：这个方案访问域名时必须加 `:8080`，且无法配置标准 HTTPS。

---

## 📊 两个方案对比

| 特性 | 方案一（推荐）| 方案二（备选）|
|-----|------------|------------|
| 域名访问 | `health-management.top` | `health-management.top:8080` |
| HTTPS | ✅ 可以配置 | ❌ 无法配置标准证书 |
| 安全性 | ✅ 高 | ⚠️ 一般 |
| 用户体验 | ✅ 好（无需端口号）| ⚠️ 一般（需要记端口）|
| 博客访问 | `IP:8888` | `IP` |
| 额外配置 | 需要改博客端口 | 需要开放 8080 端口 |

---

## ✅ 配置完成检查

### DNS 检查

```bash
ping health-management.top
```

应该返回你的服务器 IP。

### 服务检查

```bash
# 检查后端
sudo systemctl status health-backend

# 检查 AI 服务
sudo systemctl status health-ai-service

# 检查 Nginx
sudo systemctl status nginx

# 检查端口
sudo netstat -tulnp | grep :80
sudo netstat -tulnp | grep :8081
```

### 访问检查

**方案一**：
- [ ] 博客：`http://服务器IP:8888` 可访问
- [ ] 健康管理：`http://health-management.top` 可访问
- [ ] HTTPS：`https://health-management.top` 可访问（配置后）

**方案二**：
- [ ] 博客：`http://服务器IP` 可访问
- [ ] 健康管理：`http://health-management.top:8080` 可访问

---

## 🔍 常见问题

### 问题1：域名访问显示博客内容

**原因**：Nginx 配置冲突，域名被博客的 `server_name` 捕获。

**解决**：
```bash
# 检查所有 Nginx 配置
sudo nginx -T | grep "server_name"

# 确保博客配置的 server_name 是 localhost，不是域名
# 健康管理配置的 server_name 是 health-management.top
```

### 问题2：502 Bad Gateway

**原因**：后端服务未启动或端口不对。

**解决**：
```bash
# 检查后端服务
sudo systemctl status health-backend
sudo systemctl start health-backend

# 测试后端端口
curl http://localhost:8081/api/auth/health
```

### 问题3：Nginx 配置冲突

**原因**：多个配置文件监听同一个端口和域名。

**解决**：
```bash
# 查看所有配置
ls -la /etc/nginx/conf.d/
ls -la /etc/nginx/sites-enabled/

# 删除旧的或冲突的配置
sudo rm /etc/nginx/sites-enabled/default
sudo rm /etc/nginx/conf.d/default.conf

# 重启 Nginx
sudo nginx -t
sudo systemctl reload nginx
```

---

## 📝 管理命令

```bash
# 测试配置
sudo nginx -t

# 重新加载（不中断服务）
sudo systemctl reload nginx

# 完全重启
sudo systemctl restart nginx

# 查看日志
sudo tail -f /var/log/nginx/health-management-access.log
sudo tail -f /var/log/nginx/health-management-error.log
sudo tail -f /var/log/nginx/blog-access.log

# 查看所有配置
sudo nginx -T
```

---

## 🎉 完成

**推荐使用方案一**，访问更简洁，更安全！

- **博客**：`http://你的IP:8888`
- **健康管理**：`https://health-management.top` 🔒

有问题随时问我！🚀







