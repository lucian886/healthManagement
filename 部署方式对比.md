# 健康管理系统 - 部署方式对比

## 📊 两种部署方案

我为你准备了两套完整的部署方案，你可以根据实际情况选择：

| 特性 | Nginx 传统部署 ⭐ | Docker 容器化部署 |
|------|------------------|-------------------|
| **复杂度** | 简单直接 | 需要学习 Docker |
| **部署速度** | 快速 | 首次构建较慢 |
| **环境依赖** | 需要逐个安装 | 环境隔离，无需担心冲突 |
| **资源占用** | 较低 | 稍高（容器开销） |
| **迁移难度** | 较难 | 容易（打包即走） |
| **适用场景** | 单服务器部署 | 多环境、云原生部署 |
| **维护成本** | 中等 | 低（标准化） |

## 🎯 你选择的方案：Nginx 传统部署

根据你的需求，我已经为你创建了完整的 Nginx 传统部署方案。

---

## 📦 Nginx 传统部署文件清单

### 核心文件
```
healthManagement/
├── deploy-nginx.sh                    # ⭐ 一键部署脚本
├── Nginx部署指南.md                   # ⭐ 快速指南（建议先看）
├── DEPLOYMENT_NGINX.md                # 详细部署文档
│
├── nginx/
│   └── health-management.conf         # Nginx 完整配置
│
├── systemd/
│   ├── health-backend.service         # 后端 Systemd 服务
│   └── health-ai-service.service      # AI Systemd 服务
│
└── env.example                        # 环境变量模板
```

### 辅助脚本
```
scripts/
├── backup.sh                          # 数据备份
├── restore.sh                         # 数据恢复
└── health-check.sh                    # 健康检查
```

---

## 🚀 快速部署步骤

### 1️⃣ 服务器准备

```bash
# 在服务器上安装依赖
sudo apt-get update
sudo apt-get install -y openjdk-17-jdk nginx python3 python3-pip python3-venv

# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### 2️⃣ 上传代码

```bash
# 方式一：Git（推荐）
cd /opt
sudo git clone <你的仓库地址> health-management

# 方式二：SCP
# 本地执行：
tar -czf health.tar.gz healthManagement/ --exclude=node_modules --exclude=target
scp health.tar.gz root@服务器IP:/opt/

# 服务器执行：
cd /opt
tar -xzf health.tar.gz
mv healthManagement health-management
```

### 3️⃣ 配置环境

```bash
cd /opt/health-management

# 复制环境变量
cp env.example .env

# 编辑配置（填入真实的 API Key）
vi .env
```

需要配置：
```bash
DASHSCOPE_API_KEY=你的通义千问API密钥
ALIYUN_OSS_ACCESS_KEY_ID=你的OSS密钥ID
ALIYUN_OSS_ACCESS_KEY_SECRET=你的OSS密钥Secret
```

### 4️⃣ 一键部署

```bash
chmod +x deploy-nginx.sh
sudo ./deploy-nginx.sh
```

**完成！** 访问 `http://服务器IP`

---

## 📖 详细文档导航

### 开始部署
1. **Nginx部署指南.md** ⭐ - 推荐先看，简洁快速
2. **DEPLOYMENT_NGINX.md** - 详细的部署文档，包含故障排查

### 其他文档
- **部署说明.md** - Docker 部署的中文说明（如果你改变主意）
- **DEPLOYMENT.md** - Docker 部署的详细文档
- **QUICK_START.md** - Docker 5分钟快速部署

---

## 🔧 部署后管理

### 服务管理

```bash
# 后端服务
sudo systemctl start health-backend      # 启动
sudo systemctl stop health-backend       # 停止
sudo systemctl restart health-backend    # 重启
sudo systemctl status health-backend     # 状态

# AI 服务
sudo systemctl start health-ai-service
sudo systemctl stop health-ai-service
sudo systemctl restart health-ai-service
sudo systemctl status health-ai-service

# Nginx
sudo systemctl restart nginx
sudo systemctl status nginx
```

### 查看日志

```bash
# 后端日志
sudo journalctl -u health-backend -f

# AI 服务日志
sudo journalctl -u health-ai-service -f

# Nginx 日志
sudo tail -f /var/log/nginx/health-management-access.log
```

### 更新代码

```bash
cd /opt/health-management
git pull

# 重新构建前端
cd frontend
npm run build

# 重新构建后端
cd ../backend
./mvnw clean package -DskipTests

# 重启服务
sudo systemctl restart health-backend
sudo systemctl restart health-ai-service
sudo systemctl reload nginx
```

---

## 🏗️ 部署架构

### Nginx 传统部署架构

```
Internet
    ↓
Nginx (端口 80)
├── /          → 静态文件 (/opt/health-management/frontend/dist/)
├── /api/      → 反向代理到后端 (127.0.0.1:8081)
└── /ai/       → 反向代理到 AI (127.0.0.1:8001)

Systemd 服务:
├── health-backend.service     → Spring Boot (端口 8081)
└── health-ai-service.service  → FastAPI (端口 8001)

数据存储:
├── PostgreSQL (外部数据库: 47.94.41.55:5432)
├── 上传文件 (/opt/health-management/backend/uploads/)
└── 日志文件 (/opt/health-management/logs/)
```

---

## 🔍 重要文件位置

### 配置文件
| 文件 | 位置 |
|------|------|
| Nginx 配置 | `/etc/nginx/sites-available/health-management.conf` |
| 后端服务配置 | `/etc/systemd/system/health-backend.service` |
| AI 服务配置 | `/etc/systemd/system/health-ai-service.service` |
| 环境变量 | `/opt/health-management/.env` |
| 后端配置 | `/opt/health-management/backend/.../application-prod.yml` |

### 应用文件
| 内容 | 位置 |
|------|------|
| 前端静态文件 | `/opt/health-management/frontend/dist/` |
| 后端 JAR | `/opt/health-management/backend/target/*.jar` |
| Python 虚拟环境 | `/opt/health-management/ai-service/venv/` |
| 上传文件 | `/opt/health-management/backend/uploads/` |

### 日志文件
| 日志 | 位置 |
|------|------|
| 后端日志 | `journalctl -u health-backend` |
| AI 服务日志 | `journalctl -u health-ai-service` |
| Nginx 访问日志 | `/var/log/nginx/health-management-access.log` |
| Nginx 错误日志 | `/var/log/nginx/health-management-error.log` |
| 应用日志 | `/opt/health-management/logs/` |

---

## ⚡ 常用命令速查

```bash
# 查看所有服务状态
sudo systemctl status health-backend health-ai-service nginx

# 重启所有服务
sudo systemctl restart health-backend health-ai-service nginx

# 查看所有实时日志
sudo journalctl -u health-backend -u health-ai-service -f

# 测试 API
curl http://localhost/api/auth/health

# 检查 Nginx 配置
sudo nginx -t

# 重新加载 Nginx（无需重启）
sudo nginx -s reload
```

---

## 🆘 常见问题

### 1. 服务启动失败

```bash
# 查看详细错误
sudo journalctl -u health-backend -n 100 --no-pager

# 手动测试启动
cd /opt/health-management/backend
java -jar target/health-management-0.0.1-SNAPSHOT.jar
```

### 2. 页面无法访问

```bash
# 检查 Nginx 状态
sudo systemctl status nginx

# 检查防火墙
sudo ufw status
sudo ufw allow 80/tcp

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/error.log
```

### 3. API 请求 404

```bash
# 测试后端服务
curl http://localhost:8081/api/auth/health

# 检查 Nginx 代理配置
sudo cat /etc/nginx/sites-available/health-management.conf

# 测试 Nginx 配置
sudo nginx -t
```

---

## 🎯 下一步

1. **阅读快速指南**：`Nginx部署指南.md`
2. **准备服务器**：安装必要的依赖
3. **上传代码**：将项目代码上传到 `/opt/health-management`
4. **配置环境变量**：编辑 `.env` 文件
5. **执行部署**：运行 `sudo ./deploy-nginx.sh`
6. **验证部署**：访问 `http://服务器IP`

---

## 🔄 如果想切换到 Docker 部署

如果以后想改用 Docker 部署，只需：

```bash
# 停止 Systemd 服务
sudo systemctl stop health-backend health-ai-service

# 使用 Docker 部署
./deploy.sh  # 选择选项 1

# 更新 Nginx 配置以代理到 Docker 容器
# 或者直接使用 Docker 的前端容器（端口 80）
```

---

## 📞 获取帮助

- **快速参考**：`Nginx部署指南.md`
- **详细文档**：`DEPLOYMENT_NGINX.md`
- **查看日志**：`sudo journalctl -u health-backend -f`
- **健康检查**：`./scripts/health-check.sh`

---

**祝你部署顺利！** 🎉

如有问题，请查看相应文档或日志进行排查。







