# 健康管理系统 - 服务器部署说明

## 📦 已创建的部署文件

我已经为你创建了完整的 Docker 容器化部署方案，以下是所有相关文件：

### 核心部署文件

```
healthManagement/
├── docker-compose.yml          # Docker 编排配置
├── deploy.sh                   # 一键部署脚本（推荐使用）
├── env.example                 # 环境变量模板
├── .dockerignore              # Docker 构建忽略文件
│
├── ai-service/
│   └── Dockerfile             # AI 服务容器配置
│
├── backend/
│   ├── Dockerfile             # 后端服务容器配置
│   └── src/main/resources/
│       └── application-prod.yml  # 生产环境配置
│
├── frontend/
│   ├── Dockerfile             # 前端服务容器配置
│   └── nginx.conf             # Nginx 配置
│
└── scripts/
    ├── backup.sh              # 数据备份脚本
    ├── restore.sh             # 数据恢复脚本
    └── health-check.sh        # 健康检查脚本
```

### 文档文件

- **DEPLOYMENT.md** - 详细部署文档（包含故障排除）
- **QUICK_START.md** - 5分钟快速部署指南
- **部署说明.md** - 本文档（中文说明）

---

## 🚀 快速部署步骤

### 第一步：准备服务器环境

**服务器要求：**
- 操作系统：Linux (Ubuntu 20.04+ 或 CentOS 7+)
- 内存：至少 4GB RAM
- 磁盘：至少 20GB
- 端口：需开放 80、8081、8001

**安装 Docker (Ubuntu)：**

```bash
# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 启动 Docker
sudo systemctl start docker
sudo systemctl enable docker

# 安装 Docker Compose
sudo apt-get install docker-compose-plugin

# 验证安装
docker --version
docker compose version
```

### 第二步：上传代码到服务器

**方式一：使用 Git（推荐）**

```bash
# 在服务器上执行
cd /opt
git clone <你的仓库地址> health-management
cd health-management
```

**方式二：使用 SCP 上传**

```bash
# 在本地执行
cd /Users/liuzixuan/
tar -czf healthManagement.tar.gz healthManagement/
scp healthManagement.tar.gz root@你的服务器IP:/opt/

# 在服务器上执行
cd /opt
tar -xzf healthManagement.tar.gz
cd healthManagement
```

### 第三步：配置环境变量

```bash
# 复制环境变量模板
cp env.example .env

# 编辑配置
vi .env
```

**修改以下内容：**

```bash
# 通义千问 API Key（必填）
DASHSCOPE_API_KEY=sk-你的API密钥

# 阿里云 OSS 配置（必填）
ALIYUN_OSS_ACCESS_KEY_ID=你的AccessKeyId
ALIYUN_OSS_ACCESS_KEY_SECRET=你的AccessKeySecret
```

> 💡 **获取 API Key：**
> - 通义千问：https://dashscope.console.aliyun.com/
> - 阿里云 OSS：https://ram.console.aliyun.com/

### 第四步：执行部署

```bash
# 赋予执行权限
chmod +x deploy.sh

# 运行部署脚本
./deploy.sh
```

**选择 "1 - 全新部署"**

脚本会自动完成：
1. ✅ 检查 Docker 环境
2. ✅ 构建三个服务的镜像
3. ✅ 启动所有容器
4. ✅ 执行健康检查

### 第五步：验证部署

等待 2-3 分钟后，在浏览器访问：

```
http://你的服务器IP
```

你应该能看到健康管理系统的登录页面！

---

## 🎯 服务访问地址

部署成功后，可以访问：

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端应用 | http://服务器IP | 主要访问入口 |
| 后端 API | http://服务器IP:8081 | REST API |
| AI 服务 | http://服务器IP:8001 | AI 智能体 |

---

## 📊 服务管理命令

### 查看服务状态

```bash
# 查看所有容器状态
docker-compose ps

# 查看资源使用情况
docker stats

# 运行健康检查脚本
./scripts/health-check.sh
```

### 查看日志

```bash
# 查看所有日志（实时）
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f backend     # 后端
docker-compose logs -f ai-service  # AI服务
docker-compose logs -f frontend    # 前端

# 查看最近100行日志
docker-compose logs --tail=100 backend
```

### 重启服务

```bash
# 重启所有服务
docker-compose restart

# 重启单个服务
docker-compose restart backend
```

### 停止服务

```bash
# 停止所有服务
docker-compose stop

# 完全停止并删除容器
docker-compose down
```

### 更新服务

```bash
# 代码更新后重新部署
git pull                          # 拉取最新代码
docker-compose up -d --build      # 重新构建并启动

# 或使用部署脚本
./deploy.sh  # 选择选项 3 - 更新并重启
```

---

## 🔧 运维脚本

### 数据备份

```bash
# 执行数据备份（数据库 + 上传文件）
./scripts/backup.sh

# 备份文件保存在 ./backups/ 目录
# 格式：db_backup_YYYYMMDD_HHMMSS.dump
```

### 数据恢复

```bash
# 恢复数据库
./scripts/restore.sh ./backups/db_backup_20241223_120000.dump

# 恢复数据库和文件
./scripts/restore.sh \
  ./backups/db_backup_20241223_120000.dump \
  ./backups/uploads_20241223_120000.tar.gz
```

### 健康检查

```bash
# 运行完整的健康检查
./scripts/health-check.sh

# 检查内容：
# - Docker 容器状态
# - 服务端点健康
# - 系统资源使用
# - Docker 容器资源
```

---

## 🔥 常见问题解决

### 1. 端口被占用

**问题：** 启动失败，提示端口已被占用

**解决方案：**

```bash
# 查看端口占用
sudo lsof -i :80
sudo lsof -i :8081

# 方式一：停止占用进程
sudo kill -9 <PID>

# 方式二：修改端口（编辑 docker-compose.yml）
vi docker-compose.yml
# 修改 ports 配置，如 "8080:80"
```

### 2. 服务无法启动

**问题：** 容器启动后立即退出

**解决方案：**

```bash
# 查看详细日志
docker-compose logs backend

# 常见原因：
# - 环境变量配置错误（检查 .env 文件）
# - 数据库连接失败（检查数据库地址和端口）
# - API Key 无效（检查通义千问 API Key）
```

### 3. 无法访问服务

**问题：** 浏览器无法访问 http://服务器IP

**解决方案：**

```bash
# 1. 检查防火墙
sudo ufw status                    # Ubuntu
sudo firewall-cmd --list-ports     # CentOS

# 2. 开放端口
sudo ufw allow 80                  # Ubuntu
sudo firewall-cmd --add-port=80/tcp --permanent  # CentOS
sudo firewall-cmd --reload         # CentOS

# 3. 检查容器是否运行
docker-compose ps

# 4. 检查服务健康
curl http://localhost/health
```

### 4. 数据库连接失败

**问题：** 后端服务日志显示数据库连接错误

**解决方案：**

```bash
# 1. 测试数据库连接
docker-compose exec backend bash
apt-get update && apt-get install -y postgresql-client
psql -h 47.94.41.55 -U health_user -d health_db

# 2. 检查数据库配置
cat backend/src/main/resources/application-prod.yml

# 3. 确保服务器可以访问数据库（检查网络和安全组）
telnet 47.94.41.55 5432
```

### 5. AI 服务调用失败

**问题：** 聊天功能无法使用

**解决方案：**

```bash
# 1. 检查 AI 服务状态
docker-compose logs ai-service

# 2. 验证 API Key
docker-compose exec ai-service env | grep DASHSCOPE

# 3. 测试 AI 服务
curl http://localhost:8001/health
```

---

## 🔒 安全建议

### 生产环境安全配置

1. **修改数据库密码**

编辑 `backend/src/main/resources/application-prod.yml`：

```yaml
spring:
  datasource:
    password: 你的新密码
```

2. **配置 HTTPS**

使用 Let's Encrypt 免费证书：

```bash
# 安装 Certbot
sudo apt-get install certbot python3-certbot-nginx

# 申请证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo crontab -e
# 添加：0 0 * * * certbot renew --quiet
```

3. **限制端口访问**

只开放必要的端口（80, 443），关闭 8081 和 8001：

```bash
sudo ufw deny 8081
sudo ufw deny 8001
```

4. **定期备份**

设置自动备份（每天凌晨3点）：

```bash
sudo crontab -e
# 添加：0 3 * * * /opt/health-management/scripts/backup.sh
```

---

## 📈 性能优化

### 1. 增加容器资源限制

编辑 `docker-compose.yml`：

```yaml
services:
  backend:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G
```

### 2. 使用 CDN

将前端静态资源上传到 CDN，提升访问速度。

### 3. 配置 Redis 缓存

添加 Redis 服务到 `docker-compose.yml`，缓存热点数据。

---

## 📞 获取帮助

### 查看文档

- [QUICK_START.md](./QUICK_START.md) - 快速开始
- [DEPLOYMENT.md](./DEPLOYMENT.md) - 详细部署文档

### 查看日志

遇到问题时，先查看日志：

```bash
docker-compose logs -f
```

### 系统状态

运行健康检查：

```bash
./scripts/health-check.sh
```

---

## ✅ 部署检查清单

部署前检查：

- [ ] 服务器满足最低配置要求（4GB RAM, 20GB 磁盘）
- [ ] Docker 和 Docker Compose 已安装
- [ ] 已获取通义千问 API Key
- [ ] 已配置阿里云 OSS 访问密钥
- [ ] 已开放必要端口（80, 8081, 8001）
- [ ] 可以访问数据库服务器

部署后验证：

- [ ] 所有容器正常运行（`docker-compose ps`）
- [ ] 前端页面可以访问
- [ ] 用户可以注册和登录
- [ ] 聊天功能正常
- [ ] 病历上传功能正常

---

**部署完成！** 🎉

如有任何问题，请参考 [DEPLOYMENT.md](./DEPLOYMENT.md) 或查看服务日志进行排查。







