# 构建脚本速查

## 🔨 本地构建命令

### 前端构建

```bash
cd /Users/liuzixuan/healthManagement/frontend

# 安装依赖（首次或依赖更新时）
npm install

# 生产构建
npm run build

# 构建完成后，产物在：frontend/dist/
```

### 后端构建

```bash
cd /Users/liuzixuan/healthManagement/backend

# Maven 构建（跳过测试）
./mvnw clean package -DskipTests

# 或使用全局 Maven
mvn clean package -DskipTests

# 构建完成后，jar 包在：
# backend/target/health-management-0.0.1-SNAPSHOT.jar
```

---

## 📤 上传到服务器

### 上传前端

```bash
# 方式一：直接上传 dist 文件夹
cd /Users/liuzixuan/healthManagement/frontend
scp -r dist root@服务器IP:/opt/health-management/frontend/

# 方式二：先打包再上传（推荐，更快）
cd /Users/liuzixuan/healthManagement/frontend
tar -czf dist.tar.gz dist/
scp dist.tar.gz root@服务器IP:/opt/health-management/frontend/

# 在服务器上解压
ssh root@服务器IP
cd /opt/health-management/frontend
tar -xzf dist.tar.gz
```

### 上传后端

```bash
cd /Users/liuzixuan/healthManagement/backend

# 上传 jar 包
scp target/health-management-0.0.1-SNAPSHOT.jar \
    root@服务器IP:/opt/health-management/backend/

# 如果配置文件有更新，也上传
scp src/main/resources/application-prod.yml \
    root@服务器IP:/opt/health-management/backend/
```

### 上传 AI 服务（首次部署）

```bash
cd /Users/liuzixuan/healthManagement/ai-service

# 打包整个目录
tar -czf ai-service.tar.gz *.py requirements.txt

# 上传
scp ai-service.tar.gz root@服务器IP:/opt/health-management/

# 在服务器上解压
ssh root@服务器IP
cd /opt/health-management
tar -xzf ai-service.tar.gz
```

---

## 🚀 服务器端启动命令

### 启动后端

```bash
ssh root@服务器IP
cd /opt/health-management/backend

# 后台启动
nohup java -jar health-management-0.0.1-SNAPSHOT.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &

# 查看日志
tail -f backend.log

# 查看进程
ps aux | grep health-management
```

### 启动 AI 服务

```bash
ssh root@服务器IP
cd /opt/health-management/ai-service

# 设置环境变量（记得改成你的实际 API Key）
export DASHSCOPE_API_KEY="sk-your-api-key"

# 安装依赖（首次）
pip3 install -r requirements.txt

# 后台启动
nohup python3 main.py > ai-service.log 2>&1 &

# 查看日志
tail -f ai-service.log

# 查看进程
ps aux | grep main.py
```

### 配置 Nginx

```bash
ssh root@服务器IP

# 复制配置文件
sudo vi /etc/nginx/sites-available/health-management

# 粘贴 nginx/simple-nginx.conf 的内容
# 记得修改 root 路径为你的实际路径

# 创建软链接
sudo ln -s /etc/nginx/sites-available/health-management \
           /etc/nginx/sites-enabled/

# 删除默认配置（可选）
sudo rm /etc/nginx/sites-enabled/default

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

---

## 🔄 更新流程

### 更新前端

```bash
# 1. 本地重新构建
cd /Users/liuzixuan/healthManagement/frontend
npm run build

# 2. 上传（会覆盖）
tar -czf dist.tar.gz dist/
scp dist.tar.gz root@服务器IP:/opt/health-management/frontend/

# 3. 服务器解压
ssh root@服务器IP
cd /opt/health-management/frontend
rm -rf dist
tar -xzf dist.tar.gz

# 4. 清除浏览器缓存，刷新即可
```

### 更新后端

```bash
# 1. 本地重新构建
cd /Users/liuzixuan/healthManagement/backend
./mvnw clean package -DskipTests

# 2. 上传
scp target/health-management-0.0.1-SNAPSHOT.jar \
    root@服务器IP:/opt/health-management/backend/

# 3. 服务器重启后端
ssh root@服务器IP

# 停止旧进程
ps aux | grep health-management
kill <进程ID>

# 启动新版本
cd /opt/health-management/backend
nohup java -jar health-management-0.0.1-SNAPSHOT.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &
```

### 更新 AI 服务

```bash
# 1. 上传新代码
cd /Users/liuzixuan/healthManagement/ai-service
scp main.py agent.py tools.py config.py \
    root@服务器IP:/opt/health-management/ai-service/

# 2. 服务器重启
ssh root@服务器IP

# 停止旧进程
ps aux | grep "python3 main.py"
kill <进程ID>

# 启动新版本
cd /opt/health-management/ai-service
export DASHSCOPE_API_KEY="sk-your-api-key"
nohup python3 main.py > ai-service.log 2>&1 &
```

---

## 🛑 停止服务

```bash
# 停止后端
ps aux | grep health-management
kill <后端PID>

# 停止 AI 服务
ps aux | grep "python3 main.py"
kill <AI PID>

# 或者直接使用 pkill
pkill -f health-management-0.0.1-SNAPSHOT.jar
pkill -f "python3 main.py"
```

---

## 📝 一键脚本（推荐）

### 本地构建脚本 `build.sh`

```bash
#!/bin/bash

echo "🔨 开始构建..."

# 构建前端
echo "📦 构建前端..."
cd frontend
npm run build
tar -czf dist.tar.gz dist/
cd ..

# 构建后端
echo "📦 构建后端..."
cd backend
./mvnw clean package -DskipTests
cd ..

echo "✅ 构建完成！"
echo "前端: frontend/dist.tar.gz"
echo "后端: backend/target/health-management-0.0.1-SNAPSHOT.jar"
```

### 本地上传脚本 `upload.sh`

```bash
#!/bin/bash

SERVER="root@服务器IP"
REMOTE_DIR="/opt/health-management"

echo "📤 上传文件到服务器..."

# 上传前端
echo "📤 上传前端..."
scp frontend/dist.tar.gz $SERVER:$REMOTE_DIR/frontend/

# 上传后端
echo "📤 上传后端..."
scp backend/target/health-management-0.0.1-SNAPSHOT.jar \
    $SERVER:$REMOTE_DIR/backend/

echo "✅ 上传完成！"
echo "请登录服务器执行："
echo "  cd $REMOTE_DIR/frontend && tar -xzf dist.tar.gz"
echo "  cd $REMOTE_DIR/backend && ./restart-backend.sh"
```

### 服务器重启脚本 `restart-backend.sh`

```bash
#!/bin/bash

cd /opt/health-management/backend

# 停止旧进程
echo "🛑 停止旧进程..."
pkill -f health-management-0.0.1-SNAPSHOT.jar

sleep 2

# 启动新进程
echo "🚀 启动新进程..."
nohup java -jar health-management-0.0.1-SNAPSHOT.jar \
    --spring.profiles.active=prod \
    > backend.log 2>&1 &

sleep 3

# 显示状态
echo "✅ 启动完成！"
ps aux | grep health-management | grep -v grep

echo "📋 查看日志: tail -f backend.log"
```

使用方式：

```bash
# 本地执行
chmod +x build.sh upload.sh
./build.sh && ./upload.sh

# 服务器执行
chmod +x restart-backend.sh
./restart-backend.sh
```

---

## 🎯 完整流程总结

```bash
# ===== 本地操作 =====

# 1. 构建前端
cd frontend && npm run build

# 2. 构建后端
cd backend && ./mvnw clean package -DskipTests

# 3. 上传
scp -r frontend/dist root@IP:/opt/health-management/frontend/
scp backend/target/*.jar root@IP:/opt/health-management/backend/

# ===== 服务器操作 =====

# 4. 配置 Nginx（首次）
sudo vi /etc/nginx/sites-available/health-management
# 粘贴配置，修改路径
sudo ln -s /etc/nginx/sites-available/health-management /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# 5. 启动后端
cd /opt/health-management/backend
nohup java -jar health-management-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod > backend.log 2>&1 &

# 6. 启动 AI 服务
cd /opt/health-management/ai-service
export DASHSCOPE_API_KEY="your-key"
nohup python3 main.py > ai-service.log 2>&1 &

# 7. 访问
# http://服务器IP
```

---

完成！现在你有了最简单的构建和部署流程。







